# Domain pack: {{Field name}}

> Template for a new domain pack. Copy `domains/_template/` to `domains/<your-field-id>/` and fill
> it in. See [`protocol/creating-a-domain.md`](../../protocol/creating-a-domain.md) for how — it's
> an **Expert-layer** task (research the field, build the graph, curate by the field's epistemics).
> A completed pack is a contribution back to the repo (PR it so others can pick it).

**Goal it serves:** {{what the learner wants to become / be able to do}}

**Stance:** {{what mastery actually looks like here; what to optimize for; what's an edge}}

**Contents**
- `knowledge-map.md` — the competency graph (dependency-ordered, coarse → frontier).
- `state.seed.js` — the seed state (graph, mastery all `unknown`) the tutor copies on selection.
- `field-pulse.md` — a dated currency snapshot to seed the Expert layer.

**Epistemics for this field (for the Expert layer):** {{what counts as credible/current knowledge
in THIS field — e.g. which sources, venues, what's canon vs. frontier vs. hype}}

**Portfolio orientation:** {{what shippable artifacts this field rewards}}
